  # Copy to `.env` and adjust as needed; unspecified values fall back to defaults.
  VIDEO_PATHS=PATH_1,PATH_2,...,PATH_N
  ROI_CONFIG_PATH=ROI_FILE_PATH
  OUTPUT_DIRECTORY=OUTPUT_DIR
  YOLO_WEIGHTS_PATH=MODEL_PATH
  YOLO_DEVICE=
  MODEL_WS_URL=SERVER_WEBSOCKET_URL
  FRAME_INTERVAL=
  QUEUE_CAPACITY=